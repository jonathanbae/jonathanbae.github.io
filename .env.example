# Copy to .env.local and fill in. The anon key is safe to ship in the client
# bundle — every table is behind RLS and requires a signed-in session.
VITE_SUPABASE_URL=https://swfodmecqtfilndbqjdl.supabase.co
VITE_SUPABASE_ANON_KEY=
